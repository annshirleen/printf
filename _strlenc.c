#include "main.h"

/**
 * _strlenc - function that calculates length of constants for s
 * @s: string
 * Return: count
 */
int _strlenc(const char *s)
{
	int count = 0;

	while (s[c] != 0)
		c++;
	return (count);
}
